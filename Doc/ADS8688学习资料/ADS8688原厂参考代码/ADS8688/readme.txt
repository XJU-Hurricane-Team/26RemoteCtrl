本代码基于STM32标准库，#include“sys.h”为正点原子编写的头文件，包含类型的重命名，读者也可使用标准库默认的类型，
uint8_t  --->  u8    等。


使用时须将头文件和源文件同时添加进工程，代码中ADS8688_Init_Single（）为初始化开启单通道，默认为通道1，
ADS8688_Init_Mult（）为初始化开启全部通道。get_ADS_ch1（）函数为获取通道1ADC转换结果，
结果为16位数表示，32768对应电压值0V，32768-65535对应0--正最大测量电压，32768-0对应0--负最大测量电压。





